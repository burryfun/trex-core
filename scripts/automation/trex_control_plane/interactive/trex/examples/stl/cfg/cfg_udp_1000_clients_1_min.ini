[DEFAULT]
; username=user
; password=pass
number_of_clients=1000
server_ip=192.168.1.10
; SINGLE, MIX(UDP, TCP), PCAP, BIDIRECT
mode=SINGLE
; UDP, TCP(SYN)
payload=UDP
; payload duration in minutes
duration=1
pcap_file=cap2/http_get.pcap
